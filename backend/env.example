PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# === Gemini (Google Generative AI) Parsing ===
# Set your Gemini API key to enable automatic receipt parsing via LLM.
GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE

# Default parser mode: manual | gemini
RECEIPT_PARSER_MODE=gemini

# Optional: override model (defaults to gemini-1.5-flash if unset)
# GEMINI_MODEL=gemini-1.5-flash

# Include raw model output in responses (debug only)
# INCLUDE_MODEL_RAW=1
